#pragma once

// define that we want to do verbose printing
//#define VERBOSE

// define that we want to do debug checks
//#define DEBUG_CHECK

// define that we want to launch a mutex and only run once
//#define RUN_ONCE_MUTEX

// run using a self runner with HeapAlloc as the allocator
#define RUN_HEAP

// encoding your payload using dictionary words
#define ENCODE_DICT

